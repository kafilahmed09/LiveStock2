@model IEnumerable<BES.Areas.Procurement.Models.Project>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProjectName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GrantNo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProjectID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProjectName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GrantNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProjectID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProjectNo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProjectNo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProjectNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
